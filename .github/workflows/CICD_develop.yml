name: deploy develop

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'branch to deploy'
        required: true
        default: 'develop'
        type: string
      label:
        description: 'label to deploy'
        required: true
        default: 'TORDER'
        type: choice
        options:
          - ALL
          - TORDER
          - UPLUS
          - BRAND1

jobs:
  check_branch_exists:
    runs-on: ubuntu-latest
    outputs:
      branch_exists: ${{ steps.check_branch.outputs.branch_exists }}
      branch_name: ${{ steps.check_branch.outputs.branch_name }}
    steps:
      - name: Check if branch exists
        id: check_branch
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const branchName = '${{ github.event.inputs.branch }}';
            const { data: branch } = await github.request('GET /repos/{owner}/{repo}/branches/{branch}', {
              owner: context.repo.owner,
              repo: context.repo.repo,
              branch: branchName
            });
            
            if (branch) {
              core.setOutput('branch_exists', 'true');
              core.setOutput('branch_name', branchName);
              return
            }
            
            core.setOutput('branch_exists', 'false');

  deploy:
    runs-on: ubuntu-latest
    needs: check_branch_exists
    if: ${{ needs.check_branch_exists.outputs.branch_exists == 'true' }}
    steps:
      - name: Set Deployment Variables
        id: set-deployment-vars
        run: |
          echo "LABEL=${{ github.event.inputs.label }}" >> $GITHUB_ENV
          if [ "${{ github.event.inputs.label }}" == "ALL" ] || [ "${{ github.event.inputs.label }}" == "TORDER" ]; then
            echo "BUILD_COMMAND=yarn origin:build:dev" >> $GITHUB_ENV
            echo "AWS_BUCKET_NAME=${{ secrets.AWS_DEV_BUCKET_NAME }}" >> $GITHUB_ENV
            echo "AWS_DISTRIBUTION_ID=" >> $GITHUB_ENV
          elif [ "${{ github.event.inputs.label }}" == "UPLUS" ]; then
            echo "BUILD_COMMAND=yarn uplus:build:dev" >> $GITHUB_ENV
            echo "AWS_BUCKET_NAME=${{ secrets.AWS_DEV_UPLUS_BUCKET_NAME }}" >> $GITHUB_ENV
            echo "AWS_DISTRIBUTION_ID=${{ secrets.AWS_DEV_UPLUS_DISTRIBUTION_ID }}" >> $GITHUB_ENV
          elif [ "${{ github.event.inputs.label }}" == "BRAND1" ]; then
            echo "BUILD_COMMAND=yarn brand1:build:dev" >> $GITHUB_ENV
            echo "AWS_BUCKET_NAME=${{ secrets.AWS_DEV_BRAND1_BUCKET_NAME }}" >> $GITHUB_ENV
            echo "AWS_DISTRIBUTION_ID=${{ secrets.AWS_DEV_BRAND1_DISTRIBUTION_ID }}" >> $GITHUB_ENV

      - name: Check out branch
        uses: actions/checkout@v3
        with:
          ref: ${{ needs.check_branch_exists.outputs.branch_name }}

      - uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: Cache node modules
        id: node-cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.OS }}-admin-build-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-build-
            ${{ runner.OS }}-

      - name: Install Dependencies
        run: yarn

      - name: Build
        run: ${{ env.BUILD_COMMAND }}

      - name: Deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_BUCKET_NAME: ${{ env.AWS_BUCKET_NAME }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_DISTRIBUTION_ID: ${{ env.AWS_DISTRIBUTION_ID }}
        run: yarn deploy:action